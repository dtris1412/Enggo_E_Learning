{
  "name": "enggo-elearning",
  "version": "1.0.0",
  "description": "Enggo E-Learning Platform",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
